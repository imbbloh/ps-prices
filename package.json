{
  "name": "ps-sgd-backend",
  "version": "1.0.0",
  "private": true,
  "description": "PlayStation Store all-region price backend (returns JSON).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
